# Changelog

## 1.0.1

- Restore the stable internal extension identifier so Marketplace installs upgrade the existing extension instead of creating a second one
- Make the Activity Bar entry less conspicuous with a neutral `Bridge` title and a non-chat icon

## 1.0.0

- Rebrand the public extension as Stealth Chat Client
- Migrate public settings from `tsLint.*` to `stealthChat.*` with compatibility
- Add Marketplace-ready manifest metadata, docs, and media assets
- Keep image messages, connection switching, and background sync flows aligned with the new public branding
